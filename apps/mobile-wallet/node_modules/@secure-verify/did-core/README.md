# did-core

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build did-core` to build the library.

## Running unit tests

Run `nx test did-core` to execute the unit tests via [Jest](https://jestjs.io).
